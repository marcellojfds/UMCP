# Finding M0-ENV-001 — browser backend unavailable

Status: `external-owner`

Owner lane: `Environment`

## Reproduction

- Worktree/branch/SHA: `/private/tmp/umcp-roadmap-verification`,
  `roadmap/luna-verification`,
  `45ca25c15fedfd383eb96f8a04141fbe2423d3d1`
- Command: managed browser setup followed by `agent.browsers.list()`.
- Input: none; browser discovery only.
- Expected: at least one managed browser backend for a real local browser E2E.
- Observed: `[]` / “No browser is available”.

## Impact and contract

- Affected requirement: playbook browser E2E capability preflight; M1-C browser
  acceptance cannot be independently executed in this environment.
- Severity rationale: evidence gap only; no product assertion was converted to
  pass.
- Does this affect a release claim? `yes` — browser support remains unverified.

## Evidence

| Artifact | SHA-256 | Freshness |
| --- | --- | --- |
| `evidence/browser-preflight.json` | recorded in `evidence/checksums.sha256` | current |

## Request to lane owner

Provide a connected managed browser backend or CI runner that owns the web
server lifecycle. Re-run browser E2E against the integrated candidate; do not
replace it with a static render, local-file navigation, or a skipped test.
