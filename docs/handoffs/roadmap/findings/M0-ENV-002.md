# Finding M0-ENV-002 — loopback socket bind unavailable

Status: `external-owner`

Owner lane: `Environment`

## Reproduction

- Worktree/branch/SHA: `/private/tmp/umcp-roadmap-verification`,
  `roadmap/luna-verification`,
  `2c305ed1d339bec1252a087df60d38e2741235c7`
- Command: `./scripts/gate-fast`
- Expected: the local HTTP MCP contract tests bind a disposable loopback port
  and complete.
- Observed: 71 tests passed, but both
  `tests/contract/test_cloud_http.py::test_cloud_http_calls_tools_with_verified_tenant_principal`
  and
  `tests/contract/test_cloud_http.py::test_conformance_runner_executes_against_local_mcp_gateway`
  failed before server startup with
  `PermissionError: [Errno 1] Operation not permitted` at `socket.bind`.

## Impact and contract

- Affected requirement: MCP HTTP and local conformance execution in the
  Verification gate.
- Severity rationale: environment evidence gap; the tests were not converted
  to pass, skipped, or weakened.
- Does this affect a release claim? `yes` — HTTP MCP remains unverified in this
  sandbox. Stdio, fixture conformance, and non-network contract tests remain
  separately reported.

## Evidence

| Artifact | SHA-256 | Freshness |
| --- | --- | --- |
| `evidence/gate-fast.log` | recorded in `evidence/checksums.sha256` | current |

## Request to lane owner

Provide a CI or disposable runtime that permits loopback binding. Re-run the
two HTTP MCP contract tests and the full gate against the integrated candidate;
do not replace them with a skip or a static-only check.
