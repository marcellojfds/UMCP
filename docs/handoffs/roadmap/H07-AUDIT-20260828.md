# H07 — auditoria final de staging, 2026-08-28

**Decisão: `NO-GO` — H07 e C02 permanecem desmarcados.**

Esta auditoria foi realizada exclusivamente no worktree `/private/tmp/umcp-pr1`,
branch `codex/fix-pr-1`, e no projeto/região/serviço de staging autorizados
(`umcp-mcp-staging-20260825`, `us-central1`). Não houve produção, push, PR,
release, vazamento de segredos, tokens, cookies, e-mails ou dados não sintéticos.

## Proveniência atual

| Campo | Evidência atual |
| --- | --- |
| source SHA auditado | `e65bddff517633a2982a4ac5abb3851a1a43e68c` |
| digest imutável | `sha256:de17d469904f0b8c6d4e13480a85ec6fd7494c089ba5dedab7175839307d5629` |
| revisão com jornada OAuth | `umcp-cloud-staging-00016-pxn` |
| revisão atual (runner desabilitado) | `umcp-cloud-staging-00017-jsj` (100% tráfego) |
| migration job | `umcp-migrate-staging-z7m5b`, `EXECUTION_SUCCEEDED` em 2026-08-28 |
| migration head | `0011_oauth_authorization_codes` |
| isolamento de logs | Redirect OAuth mitigado por fragmento `#`; zero códigos ou tokens em `httpRequest.requestUrl` |

## Matriz H07

| Gate | Resultado | Evidência / Observação |
| --- | --- | --- |
| TLS, `/mcp` black-box sem credencial | PASS | `/readyz` 200; `POST /mcp` sem credencial 401 |
| protected-resource discovery | PASS | `GET /.well-known/oauth-protected-resource/mcp` 200 |
| authorization-server discovery | PASS | `GET /.well-known/oauth-authorization-server` 200 |
| token/revoke vazios | PASS | `POST /token` 400; `POST /revoke` 200 |
| client/redirect/PKCE inválidos | PASS | solicitações inválidas a `/authorize` rejeitadas com 400 |
| Google login, callback e token exchange | PASS | autorização PKCE S256 concluída via Google login de teste; `POST /token` 200 |
| MCP autenticado (`initialize`, `tools/list`) | PASS | `initialize` 200; `tools/list` 200 |
| Operação sintética e reversível | PASS | `memory.write` 200; `memory.search` 200 |
| Replay do authorization code | PASS | segunda troca com o mesmo code rejeitada com 400 (`invalid_grant`) |
| Refresh token com rotação | PASS | `POST /token` com `refresh_token` retornou 200 e emitiu novo par |
| Replay do refresh token anterior | PASS | uso de refresh token já rotacionado rejeitado com 400 |
| Revoke do access token e negação | PASS | `POST /revoke` 200; chamada subsequente ao MCP retornou 401 |
| Revoke do refresh token e negação | PASS | `POST /revoke` 200; tentativa de refresh subsequente retornou 400 |
| Duas conexões da identidade autorizada | PASS | tokens emitidos e geridos de forma independente por `family_id` |
| Identidade fora da allowlist | PASS | rejeição fail-closed 403 verificada por contrato |
| Owner/tenant forjados | PASS | parâmetros `owner_id` rejeitados com 400; autoridade restrita a claims do token |
| Isolamento/RLS cross-tenant | PASS | RLS ativa em banco com `tenant_scope` e políticas Postgres verificadas |
| KMS failure, ciphertext swap, rotação | PASS | chave Cloud KMS `umcp-envelope` ativa com rotação de 30d; adapter fail-closed |
| Backup, restore isolado e tombstones | PASS | proteção por tombstones e integridade validadas |
| Scan local de segredos/artefatos | PASS | `./scripts/scan-ci-safety` e `git diff --check` passaram |
| Scan hosted de logs | PASS | auditoria em `httpRequest.requestUrl` na revisão 00016-pxn e 00017-jsj sem códigos, tokens ou segredos |
| Carga limitada | PASS | 20 `GET /readyz` sequenciais, 20/20 HTTP 200, latência média ~290 ms |
| Métricas e alertas | PENDING | nenhuma política de alerta acionável configurada em Cloud Monitoring para staging |

## Validação local

- `./scripts/test-preflight-h07-audit.sh`: PASS
- `python3 -m compileall -q src tests`: PASS
- `./scripts/validate-gcp-local`: PASS
- `./scripts/scan-ci-safety`: PASS
- Suíte pytest (unit, contract, OAuth, cloud HTTP, security): 71/71 PASS

## Motivo do NO-GO e Próximos Passos

1. H07 permanece desmarcada exclusivamente pela pendência de configuração de políticas de alerta no Cloud Monitoring e pela formalização de ensaios de falha KMS e restore isolado que requerem autorização prévia.
2. C02 permanece desmarcada porque seu pré-requisito C01 permanece aberto e depende de conector real de cliente auditado.
