# H07 — auditoria final de staging, 2026-08-28

**Decisão: `NO-GO` — H07 e C02 permanecem desmarcados.**

Esta auditoria foi feita exclusivamente no worktree `/private/tmp/umcp-pr1`,
branch `codex/fix-pr-1`, e no projeto/região/serviço de staging autorizados.
Não houve produção, push, PR, release, mudança de IAM/rede/KMS/OAuth,
segredo, token, cookie, e-mail, usuário real ou dado não sintético.

## Proveniência atual

| Campo | Evidência atual |
| --- | --- |
| source SHA | `3c62f2ebd55fb62710a82e89e6f108c4281ea2aa` (label da revisão) |
| digest imutável | `sha256:42e6654a09d7bbc9a36d2f3e58a2c3e2be738582590d1e8491c7a0961bdd1923` |
| revisão | `umcp-cloud-staging-00014-j72` |
| tráfego | 100% para `00014-j72` |
| migration job | `umcp-migrate-staging-525zs`, `EXECUTION_SUCCEEDED` em 2026-08-28T18:50:12Z |
| migration head | `0011_oauth_authorization_codes`, conforme estado de rollout fornecido; o job atual no mesmo digest concluiu, mas a auditoria não imprimiu saída de banco/log de migration |

O worktree estava limpo antes da redação deste handoff. O checkout principal
permaneceu sujo e não foi modificado. O HEAD desta branch inclui a evidência
em `794f185`; o código hospedado auditado é o SHA acima, não um SHA inferido
do HEAD local.

## Matriz H07

| Gate | Resultado | Evidência / limitação |
| --- | --- | --- |
| TLS, `/mcp` black-box sem credencial | PASS | `/readyz` 200; `POST /mcp` sem credencial 401, sem corpo retido |
| protected-resource discovery | PASS | `GET /.well-known/oauth-protected-resource/mcp` 200 |
| authorization-server discovery | PASS | `GET /.well-known/oauth-authorization-server` 200 |
| token/revoke vazios | PASS | `POST /token` 400; `POST /revoke` 200 |
| client/redirect/PKCE inválidos | PASS | duas solicitações inválidas a `/authorize`, ambas 400 |
| Google login, callback e token exchange | NOT-RUN | navegador in-app recebeu `ERR_BLOCKED_BY_CLIENT` no domínio Cloud Run; Chrome não estava conectado. Nenhum código, token, cookie ou identidade foi exposto ou registrado |
| MCP autenticado (`initialize`, `tools/list`, operação reversível) | NOT-RUN | depende da jornada OAuth autorizada |
| duas conexões da identidade autorizada | NOT-RUN | depende da jornada OAuth autorizada |
| state único/replay e expiração | NOT-RUN | requer callback OAuth controlado; nenhum state de teste foi persistido nesta auditoria |
| code único/replay e expiração | NOT-RUN | requer autorização completa controlada |
| refresh com rotação e revogação | NOT-RUN | requer token temporário em memória durante jornada autorizada |
| token expirado/revogado e escopo insuficiente | NOT-RUN | requer token temporário emitido na mesma revisão |
| identidade fora da allowlist | NOT-RUN | não há segunda identidade sintética/permitida para o teste sem usar usuário real |
| owner/tenant forjados | NOT-RUN | requer chamada MCP autenticada na mesma revisão |
| isolamento/RLS cross-tenant | NOT-RUN | requer duas identidades/dados sintéticos e acesso de auditoria definido; não foi inferido acesso ao banco |
| KMS failure, ciphertext swap, key swap/rotação | NOT-RUN | não foi autorizado método de falha nem operação KMS remota; nenhum material de chave foi lido ou alterado |
| backup, restore isolado e tombstones | NOT-RUN | não foi autorizado um alvo isolado/mecanismo recuperável nem foram alterados dados |
| scan local de segredos/artefatos | PASS | `./scripts/scan-ci-safety` passou; `git diff --check` passou |
| scan hosted de logs | PARTIAL | logs da revisão existem; busca redigida em `httpRequest.requestUrl` e `textPayload` não encontrou padrões de segredo/token/cookie/e-mail/query OAuth. Campos JSON estruturados não foram inspecionados para não expor payloads |
| carga limitada | PASS | 20 `GET /readyz` sequenciais, 20/20 HTTP 200, latência agregada 6.104 ms; nenhum corpo registrado |
| métricas e alertas | FAIL | consulta atual de políticas Cloud Monitoring não retornou política; não há evidência de alerta necessário configurado/acionável |

## Validação local

- `./scripts/test-preflight-h07-audit.sh`: PASS.
- `python3 -m compileall -q src tests`: PASS.
- `./scripts/validate-gcp-local`: PASS.
- `./scripts/scan-ci-safety`: PASS.
- suíte pytest OAuth/cloud: `environment-blocked`; o Python disponível não
  possui `pytest`, e nenhuma dependência foi instalada durante a auditoria.

## Blockers e menor próximo passo seguro

1. Disponibilizar um navegador conectado que alcance o domínio Cloud Run. Na
   página Google, o mantenedor deve apenas selecionar/autorizar a identidade
   de teste já aprovada; a sessão não deve copiar código ou token.
2. Antes da reauditoria, prover a autorização e o mecanismo recuperável para
   dados sintéticos de RLS, ensaio KMS e restore isolado, além de evidência de
   alertas configurados. Não inferir esses controles de IaC, healthcheck ou
   evidência histórica.
3. Reexecutar todos os gates dependentes da jornada na mesma revisão/digest,
   ou reiniciar a matriz se houver nova revisão.

H07 não pode ser marcada: há gates `NOT-RUN` e o gate de alertas está `FAIL`.
`C02` também permanece desmarcada: além de H07 não estar concluída, seu
pré-requisito próprio `C01` está aberto e não existe conector real controlado
auditado.
