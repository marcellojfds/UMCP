# Own-agent recipe

Use the existing Python SDK for the available local stdio Alpha path. The
TypeScript package is a transport-agnostic scaffold: provide an authenticated
MCP transport rather than placing a bearer token in source or browser code.
Run the conformance runner from a clean environment and preserve only redacted
output.
